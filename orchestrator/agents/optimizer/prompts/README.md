# Prompt templates

These prompts are for using an LLM to produce an OptimizerReport that matches `schema/output.schema.json`.

- `system.md`: system prompt
- `user.md`: user prompt template (replace the `{{...}}` placeholders)

The module also exports these templates as strings via `PROMPTS`.
